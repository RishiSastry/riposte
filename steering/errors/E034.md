# E034 — wrong number of arguments

**Cause:** You called a predicate with too many or too few arguments.

**Broken:** `can_ko(my.active)`            (missing the defender)
**Fixed:**  `can_ko(my.active, opponent.active)`

See `predicate_reference("<name>")` for the exact arity.
