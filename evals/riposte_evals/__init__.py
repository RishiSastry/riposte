"""Riposte-specific eval bindings for evalkit (step definitions + baselines).

Importing `riposte_evals.steps` registers the Gherkin step definitions with evalkit's global
registry — that's what `evalkit run --steps riposte_evals.steps` does.
"""
